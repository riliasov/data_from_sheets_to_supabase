# Data from Google Sheets to Supabase

ETL-система для автоматизированной загрузки данных из Google Sheets в PostgreSQL (Supabase) и построения аналитических витрин.

## 🎯 Возможности

- **ETL Pipeline**: Автоматическая загрузка данных из Google Sheets в staging таблицы Supabase
- **Data Marts**: Построение аналитических витрин с агрегацией продаж, списаний и балансов клиентов
- **Export to Sheets**: Автоматический экспорт готовых витрин обратно в Google Sheets для удобного просмотра
- **Incremental Loading**: Загрузка только новых/измененных данных (hash-based)
- **Centralized Logging**: Структурированное логирование всех операций (консоль + файл)
- **Type-safe Configuration**: Типизированная конфигурация с автоматической валидацией (Pydantic)

## 📁 Структура проекта

```
src/
├── config.py          # Конфигурация (Pydantic)
├── logger.py          # Централизованное логирование
├── db.py              # Подключение к БД
├── sheets.py          # Google Sheets API
├── etl/               # ETL компоненты
│   ├── data_cleaner.py
│   └── loader.py      # Загрузка в staging (hash-based)
├── data_marts/        # Построение витрин
│   ├── aggregator.py  # Pandas-агрегация
│   └── exporter.py    # Экспорт в Sheets
└── pipelines/         # Пайплайны
    ├── historical_sync.py
    ├── current_sync.py
    ├── references_sync.py
    └── sync_data_marts.py

tests/
├── unit/              # Юнит-тесты (pytest)
└── scripts/           # Вспомогательные скрипты

logs/                  # Логи выполнения
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать окружение
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt
```

### 2. Настройка Google Sheets API

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте проект или выберите существующий
3. Включите **Google Sheets API** и **Google Drive API**
4. Создайте Service Account:
   - APIs & Services → Credentials → Create Credentials → Service Account
   - Создайте ключ (JSON формат)
5. Сохраните JSON файл в папку `secrets/`
6. Поделитесь каждой Google таблицей с email из поля `client_email` в JSON файле

### 3. Настройка переменных окружения

Создайте файл `secrets/.env`:

```bash
SUPABASE_DB_URL=postgresql://user:password@host:port/dbname
GOOGLE_SHEETS_CREDENTIALS_FILE=secrets/your_credentials.json
```

### 4. Настройка источников данных

Файл `src/sources.json` содержит конфигурацию источников:

```json
{
  "sales_hst": {
    "spreadsheet_id": "YOUR_SPREADSHEET_ID",
    "sheet_names": ["Sales"],
    "gid": "0"
  }
}
```

**Где найти `spreadsheet_id`?**  
В URL таблицы: `https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit`

## 📊 Использование

### Тестирование подключений

```bash
# Проверка подключения к БД
python scripts/test_db_connection.py

# Проверка подключения к Google Sheets
python scripts/test_sheets_connection.py
```

### Запуск ETL

```bash
# Все источники
python main.py --scope=all

# Только исторические данные
python main.py --scope=historical

# Только текущие данные
python main.py --scope=current

# Только справочники
python main.py --scope=references
```

### Построение витрин данных

```bash
# Построение и экспорт витрин в Google Sheets
python src/pipelines/sync_data_marts.py
```

Витрина включает:
- Продажи по клиентам
- Списания (посещения) по клиентам
- Баланс (приобретено - списано)
- Возраст детей, дни рождения
- Телефоны клиентов

## 🧪 Тестирование

```bash
# Запуск всех тестов
pytest

# Запуск с детальным выводом
pytest -v

# Запуск конкретного теста
pytest tests/unit/test_aggregator.py
```

## 🏗️ Архитектура

### ETL Pipeline

1. **Extract**: Чтение данных из Google Sheets
2. **Transform**: Нормализация названий клиентов, очистка данных
3. **Load**: Инкрементальная загрузка в staging (только новые/измененные строки)

### Data Marts Pipeline

1. **Aggregation**: Pandas-агрегация из staging таблиц (локально, минимум нагрузки на БД)
2. **Calculation**: Расчет балансов, возраста, birthday alerts
3. **Export**: Экспорт готовых витрин в Google Sheets

## 🔧 Технологический стек

- **Python 3.13**
- **PostgreSQL** (Supabase)
- **Pandas** - агрегация данных
- **SQLAlchemy** - работа с БД
- **gspread** - Google Sheets API
- **Pydantic** - типизированная конфигурация
- **pytest** - тестирование

## 📝 Логирование

Логи сохраняются в:
- **Консоль**: INFO уровень
- **Файл**: `logs/etl_YYYYMMDD.log` (все уровни)

Формат: `YYYY-MM-DD HH:MM:SS | LEVEL | module | message`

## 🎨 Особенности реализации

### Инкрементальная загрузка
- Использует MD5-хеш строки для определения изменений
- Загружает только новые/измененные данные
- Существенно ускоряет повторные запуски

### Нормализация имен клиентов
- Удаление лишних пробелов, табуляций
- Приведение к lowercase для устранения дубликатов
- Канонический ключ для merge операций

### Birthday Alerts
- Автоматическое определение дней рождения детей в диапазоне [-3, +7] дней
- Учет переходов между годами
- Извлечение имени ребенка из `imya_rebenka` или `klient_id`

### Форматирование возраста
- Дети < 1 года: "N мес"
- Дети 1-5 лет: "N,M года" (десятичный формат)
- Дети ≥ 5 лет: "N лет" (целое число)

## 📅 Разработка

### Запуск dev-окружения

```bash
source venv/bin/activate
```

### Структура коммитов

```
feat: Добавлена новая функция
fix: Исправлен баг
refactor: Рефакторинг кода
test: Добавлены тесты
docs: Обновление документации
```

## 📜 История версий

### v1.0 (Текущая версия)
- ✅ ETL Pipeline (historical, current, references)
- ✅ Data Marts с агрегацией
- ✅ Export витрин в Google Sheets
- ✅ Централизованное логирование
- ✅ Типизированная конфигурация (Pydantic)
- ✅ Unit-тесты (pytest)
- ✅ Birthday alerts и форматирование возраста

## 🤝 Разработчики

Проект создан для автоматизации аналитики данных детского центра.

## 📄 Лицензия

Внутренний проект.
